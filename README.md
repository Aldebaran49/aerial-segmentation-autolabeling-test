# Авторазметка и семантическая сегментация аэроснимков

## 🛠 Установка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/ВАШ_НИК/aerial-segmentation-auto-labeling.git
    cd aerial-segmentation-auto-labeling
    ```

2.  **Установите зависимости:**
    Рекомендуется использовать Python 3.8+.
    ```bash
    pip install -r requirements.txt
    ```
    *Примечание: Для работы с GPU убедитесь, что у вас установлен PyTorch с поддержкой CUDA.*

3.  **Скачайте веса (Для DeepLabV3+):**
    Модель SegFormer и SAM скачивают веса автоматически. 
	Для DeepLabV3+ необходимо скачать файл `best_deeplabv3plus_resnet101_cityscapes_os16.pth` 
	([Ссылка](https://github.com/VainF/DeepLabV3Plus-Pytorch)) и положить его в папку `weights/`.

## 🚀 Запуск

### Полный цикл валидации
Скрипт `main.py` запускает прогон по всему датасету, считает метрики (mIoU, mPA) и сохраняет примеры визуализации.

Откройте `main.py` и настройте параметры в блоке конфигурации:
```
DATASET_NAME = "uavid"      # "aeroscapes" или "uavid"
MODEL_NAME = "segformer"    # "segformer", "deeplab" или "sam"
```

Запуск:
```
python main.py
```

### Тест Grounded-SAM на одном изображении
Для отладки промптов и проверки работы SAM используйте:
```bash
python sam_test.py
```
## 📂 Структура проекта

*   `data/` - Папка для датасетов (не включена в git).
*   `src/`
    *   `datasets/` - Загрузчики данных и конвертеры JSON.
    *   `models/` - Обертки для SegFormer, DeepLab и Grounded-SAM.
    *   `evaluation/` - Расчет метрик.
    *   `utils.py` - Словари маппинга классов и конфиги промптов.
*   `weights/` - Веса моделей.
*   `main.py` - Точка входа.
